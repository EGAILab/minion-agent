name: an unknown tool name does not serialize the batch
description: >
  An unresolvable name never runs, so it has no exclusivity to spread.
  Treating it as sequential would let one model typo serialize an otherwise
  parallel batch -- a performance cliff with no semantic justification.

  The unknown call still receives a result, as every call must.

  Also carries `IR-L06-003`/`IR-L06-004`'s cross-language canonical evidence: pinned Pi's
  unresolvable-tool text is exactly `` `Tool ${toolCall.name} not found` `` -- for `nonexistent`,
  exactly "Tool nonexistent not found" -- and pinned Pi's `createErrorToolResult` sets
  `details: {}` (an empty object, never absent), which survives unchanged into the model-visible
  `ToolResultMessage.details`. Both are asserted below by exact equality, not substring
  containment, and must hold identically in Python and (once implemented) Rust.

  `CA-L06-007`, `CONTRACT_ASSURANCE_DEFECT` (independent PASS-2 Rust review): the `details: {}`
  assertion belongs ONLY to the generated error -- `createErrorToolResult` is the one Pi source
  that supplies it. `slow`/`quick` are ordinary successful results whose fixtures declare only
  `result.text`; pinned Pi's `AgentToolResult<T>.details: T` is required but never defaulted by
  the pipeline itself for a successful outcome -- a real tool that returns no details of its own
  is not something Pi's own execution code ever synthesizes a value for. A prior revision of this
  scenario asserted `details: {}` on both successful results too, which nothing in the fixture (or
  Pi) actually justified -- the corrected canonical schema has no `details` member on `toolStub`
  at all, so a runner satisfying that assertion would have had to invent a host default and pass
  it off as shared semantics. Removed; `details` is asserted only where the fixture -- or Pi's own
  error helper -- actually supplies a value.
provider_script:
  - content:
      - { type: tool_call, id: t1, name: slow, arguments: {} }
      - { type: tool_call, id: t2, name: nonexistent, arguments: {} }
      - { type: tool_call, id: t3, name: quick, arguments: {} }
    stop_reason: tool_use
  - content: [{ type: text, text: done }]
    stop_reason: stop
tools:
  slow: { result: { text: slow }, delay_ticks: 1 }
  quick: { result: { text: quick } }
steps:
  - followup: go
  - await_idle: true
expect_tool_completion_order: [t2, t3, t1]
expect_messages:
  - { role: user, text: go }
  - { role: assistant, text: "" }
  - { role: tool_result, text: slow }
  - { role: tool_result, text: "Tool nonexistent not found", details: {} }
  - { role: tool_result, text: quick }
  - { role: assistant, text: done }

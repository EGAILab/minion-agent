# Rust XFORM-R005 Delta Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reproduce pinned Pi's empty-string ToolCall/ToolResult normalization asymmetry in Rust and consume the fourteenth canonical XFORM scenario.

**Architecture:** Keep the existing typed `transform_messages` seam and all map/orphan behavior in `llm/transform.rs`. Change only the real-result rewrite predicate; the callback still records and applies an empty mapping to the ToolCall. The canonical runner remains generic and changes only its repository-truth inventory assertion.

**Tech Stack:** Rust, Cargo, serde YAML/JSON canonical adapter.

**Spec:** `spec/target-model-transformation.md` rule 13 at `minion-agent-docs@f2fb2d544cf2e847d197f294a2394eb937714705`.

## Global Constraints

- Preserve `old-id -> ""` in the map and rewrite the ToolCall to `""`.
- Rewrite a real ToolResult only when the mapped value is non-empty and differs from its current ID.
- Let the existing orphan pass synthesize the unmatched empty-ID result.
- Do not add XFORM semantics to the runner or implement Layer 05/provider behavior.
- Do not modify shared spec, canonical YAML, Python, or manifest artifacts.

---

### Task 1: Direct Rust regression and minimal semantic fix

**Files:**
- Modify: `minion-agent-rust/crates/minion-agent/tests/llm_transform.rs`
- Modify: `minion-agent-rust/crates/minion-agent/src/llm/transform.rs`

**Interfaces:**
- Consumes: `transform_messages(&[Message], &TransformTarget, Option<&mut dyn ToolCallIdNormalizer>)`
- Produces: Pi-equivalent empty-string output while retaining all existing normalizer behavior.

- [ ] **Step 1: Write the failing real-seam test**

Add a test that supplies one cross-model call/result pair and a normalizer returning `""`. Assert three literal outputs: transformed call ID `""`, real result ID `"old-id"`, and synthetic error result ID `""` with `tool_name == "lookup"` and text `"No result provided"`.

- [ ] **Step 2: Verify RED**

Run:

```text
cargo test -p minion-agent --test llm_transform normalizer_returning_empty_string --all-features -- --exact
```

Expected: FAIL because the current implementation rewrites the real result to `""` and produces only two messages.

- [ ] **Step 3: Implement the minimal predicate change**

In the `Message::ToolResult` arm, rewrite only when a mapping exists, is non-empty, and differs from the current ID. Do not change ToolCall mapping.

- [ ] **Step 4: Verify GREEN and focused regressions**

Run the new test and the full `llm_transform` integration test. Expected: all pass.

### Task 2: Consume the fourteenth canonical XFORM scenario

**Files:**
- Modify: `minion-agent-rust/crates/minion-agent/tests/xform_conformance.rs`

**Interfaces:**
- Consumes: the existing dynamic canonical discovery and real `transform_legacy_messages` path.
- Produces: 14 discovered/executed canonical cases with no scenario-specific semantic branch.

- [ ] **Step 1: Update only the inventory assertion from 13 to 14**

- [ ] **Step 2: Run XFORM conformance**

```text
cargo test -p minion-agent --test xform_conformance --all-features -- --nocapture
```

Expected: 14/14 scenario loop passes, including `tool-call-id-normalization-empty-string.yaml`.

- [ ] **Step 3: Run Session conformance**

```text
cargo test -p minion-agent --test session_conformance --all-features -- --nocapture
```

Expected: 20/20 passes with no Session changes.

### Task 3: Verify, review, integrate, and publish evidence

**Files:**
- Modify after merge: `minion-agent-docs/assurance/layers/04-target-model-transformation-rust-implementation.md`

**Interfaces:**
- Produces: independently reviewed Rust delta PR, post-merge evidence, and shared-owner handoff.

- [ ] **Step 1: Run full Rust gates**

Run formatting, strict Clippy, all-feature tests, rustdoc warnings-as-errors, and xtask conformance verification.

- [ ] **Step 2: Request an independent narrow implementation review**

Review empty mapping retention, result truthiness, orphan consequence, ordinary normalization regressions, runner thinness, and Layer-05 exclusion.

- [ ] **Step 3: Open and merge a dedicated PR only after approval**

- [ ] **Step 4: Rerun all gates on merged main**

- [ ] **Step 5: Append delta history without rewriting PR #7's historical 13/13 evidence**
